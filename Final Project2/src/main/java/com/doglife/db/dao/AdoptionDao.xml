<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doglife.db.dao.AdoptionDao">

	<update id="addAdoptionResult" parameterType="Integer">
		update Customer set
		testresult = #{a} where CustomerId = #{customerId}
	</update>


	<select id="boardListSelect"
		parameterType="com.doglife.db.dto.ListDto"
		resultType="com.doglife.db.dto.BoardDto">
		SELECT * FROM announcement
		<if test="colname != null and colname != ''">
			WHERE ${colname} LIKE concat('%',#{keyword},'%')
		</if>
		LIMIT #{pageNum}, #{listCnt}
	</select>
</mapper>